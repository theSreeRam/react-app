
// PascalCasing -> Capitalize first letter of every word => Used for every function
function Message() {

    // JSX: javascript xml -> code compiled to JS only
    const name = 'Sreeram';
    if(name)
        return <h1>Hello {name}</h1>;
    return <h1>Hello World</h1>;
}

export default Message;